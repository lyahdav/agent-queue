[project]
name = "agent-queue"
version = "0.1.0"
description = "Multi-project Google Sheets-backed agent queue runner"
requires-python = ">=3.10"

[project.scripts]
agentq = "agentq.cli:main"

[tool.pytest.ini_options]
testpaths = ["tests"]
